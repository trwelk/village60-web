/** Limits for **29c** home expense attachments (shared by API service and UI copy). */
export const MAX_HOME_EXPENSE_ATTACHMENTS_PER_EXPENSE = 10;

/** Per-file size cap in bytes (default 10 MiB). */
export const MAX_HOME_EXPENSE_ATTACHMENT_FILE_BYTES = 10 * 1024 * 1024;
