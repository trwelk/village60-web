# Required for sessions (iron-session): at least 32 characters.
# Copy this file to .env.local (Next loads it for both dev and next start).
SESSION_PASSWORD=replace-with-a-long-random-secret-at-least-32-chars

# Local-only: if you run `next start` without .env.local, you may set this to 1
# in the shell (never use in production).
# ALLOW_INSECURE_SESSION_PASSWORD=1

# Optional: defaults to ./data/village60.sqlite under this app folder.
# DATABASE_PATH=./data/village60.sqlite

# Internal cron (16b): required for POST /api/internal/cron/generate-monthly-charges
# and POST /api/internal/cron/generate-monthly-salary-accruals
# (Authorization: Bearer <same value>). Omit in dev if you do not call those routes.
# CRON_SECRET=replace-with-a-long-random-secret

# Optional: app reporting timezone (defaults to UTC).
# APP_TIMEZONE=UTC

# Optional: absolute origin for resident QR codes (e.g. https://village60.example.com).
# When unset, QR routes derive the host from the incoming request.
# APP_BASE_URL=https://village60.example.com

# Optional: destination for `npm run db:backup` (defaults to ./data/backups).
# BACKUP_DIR=./data/backups

# Optional: base directory for home expense receipt files (29c). Defaults to ./data/expense-attachments.
# Use an absolute path on the server with a persistent disk.
# Optional: base directory for resident portrait images (**33a**). Defaults to ./data/resident-portraits.
# RESIDENT_PORTRAITS_DIR=/var/lib/village60/resident-portraits

# Note: `npm run db:seed` uses password `admin` for all seeded users (admin@example.com and care staff).
